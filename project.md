# project.md — AI Attendance System

---

## Что это за проект

AI Attendance System — сервис автоматического учёта рабочего времени сотрудников через распознавание лиц на фото. Бот добавляется в уже существующую Telegram-группу объекта, где сотрудники кидают фото при входе и выходе. Агент сам распознаёт лица, фиксирует время и формирует готовый табель — без ручной работы.

---

## Для кого

**Клиент / ниша:** строительная компания среднего масштаба (генподрядчик).

- 5 активных объектов
- ~120 рабочих и ИТР
- Руководители объектов вручную ведут учёт рабочего времени

---

## Проблема

- Учёт ведётся в Excel и на бумаге → постоянные ошибки
- Дубли сотрудников и «мёртвые души» в табелях
- Руководитель тратит 1–2 часа в день на сверку
- Конфликты с бухгалтерией и перерасход ФОТ
- Нет прозрачности: кто реально был на объекте и сколько работал

---

## Решение

Внедрили сервис учёта по фото:

- Сотрудники фиксируются через фото при входе/выходе
- Агент автоматически распознаёт лица и отмечает время
- Убирает дубли, формирует готовый табель
- Руководитель получает итог без ручной проверки

### Поток данных:
```
Рабочий отправляет фото в Telegram-группу с подписью
("Иван начало смены" или "Иван конец смены 18:15")
    ↓
Telegram доставляет update на webhook
    ↓
[Vercel Serverless Function — api/webhook.js, ~3 сек]
  1. Валидация секретного токена
  2. Разобрать подпись: имя, тип события (приход/уход), время
     (парсинг текста — без OCR)
  3. Скачать фото из Telegram
  4. Сжать до ~150 КБ (sharp)
  5. Сохранить фото в Supabase Storage (bucket: photos)
  6. Записать event в Supabase (status: pending)
    ↓
[GitHub Actions — ai-worker/process_events.py, каждые 5 минут]
  7.  Атомарно захватить pending-события (→ status: processing)
  8.  Восстановить зависшие события (processing > 15 мин → pending)
  9.  Найти сотрудника по name_from_photo (точное + fuzzy-match)
  10. Face recognition: лицо на фото vs эталон сотрудника
  11. Проверить дубли (два одинаковых события за день → duplicate_event)
  12. Собрать fraud_flags (face_mismatch, time_mismatch, duplicate_event)
  13. Определить event_type из подписи
  14. Рассчитать часы (последний уход − первый приход за день)
  15. Закрыть событие: status=done или needs_review
  16. Уведомить руководителя в Telegram при needs_review
  17. Записать бизнес-событие в таблицу logs
    ↓
[Vercel Dashboard — time-tracking-service-beta.vercel.app]
  Руководитель видит табель, проверяет needs_review, скачивает Excel/PDF
```

---

## Результат

| Метрика | До | После |
|---|---|---|
| Время на учёт | ~1,5 часа/день | 10–15 минут/день |
| Ошибки и дубли | Постоянно | Практически нулевые |
| Оптимизация ФОТ | — | −8–12% |
| Закрытие месяца | 3 дня | Несколько часов |
| Прозрачность присутствия | Отсутствует | Полная |

---

## Ценность для клиента

- **Руководитель** освобождает время и фокусируется на стройке, а не на бумажках
- **Собственник** получает контроль без лишних проверок
- **Бухгалтерия** работает с точными данными, без конфликтов
- **Компания** снижает потери денег и управляет персоналом на основе фактов

> Вы не просто автоматизируете табель — вы убираете целый слой ручной, раздражающей и дорогой работы.

---

## Архитектура системы

### 1. Telegram Bot — Vercel Serverless Function (`api/webhook.js`)
- Участник существующей рабочей Telegram-группы
- Принимает фото от рабочих: парсит подпись, сжимает, сохраняет в Supabase
- Тонкий обработчик: ~3 секунды на событие, без тяжёлых операций
- Размещён на Vercel (Node.js)
- URL: `https://time-tracking-service.vercel.app/api/webhook`

### 2. Supabase (бэкенд и хранилище)
- **PostgreSQL:** таблицы `employees`, `events`, `logs`
- **Storage:** bucket `photos` (фото событий), `ref-photos` (эталонные фото)
- **RLS:** доступ только для аутентифицированных пользователей
- URL: `https://weqvsquaftkxrafybrnk.supabase.co`

### 3. AI Worker — GitHub Actions (`ai-worker/process_events.py`)
- Запускается по cron каждые 5 минут
- Берёт события со статусом `pending`, распознаёт лица, считает часы
- Финализирует события: `done` или `needs_review`
- Уведомляет руководителя в Telegram при `needs_review`

### 4. Cleanup Worker — GitHub Actions (`ai-worker/cleanup_photos.py`)
- Запускается раз в сутки
- Удаляет фото из Storage старше 60 дней
- Записи в `events` остаются навсегда

### 5. Vercel Dashboard (Frontend)
- URL: `https://time-tracking-service-beta.vercel.app/`
- Страницы: Обзор, Посещаемость, Проверка, Табель, Аналитика, Логи, Сотрудники (7 страниц)
- Данные подтягиваются из Supabase напрямую из браузера

---

## Формат фото и извлечение данных

Рабочий снимает селфи и отправляет в Telegram-группу с подписью — имя + тип
события (+ опционально время).

### Пример сообщения

```
[фото селфи рабочего]

Подпись: "Иван начало смены 08:15"
         ^^^^  ^^^^^^^^^^^^^ ^^^^^
         имя   тип события   время (опц.)
```

### Что система извлекает

**Из подписи к фото (парсинг текста, не OCR):**
- **Имя** — кто отметился (ищется в базе сотрудников, допускаются опечатки)
- **Тип события** — «начало смены» (arrival) или «конец смены» (departure). Поддерживаются варианты с ошибками: «канца», «канец», «приход», «уход»

**Время события (приоритет):**
1. EXIF-метаданные фото (дата съёмки с камеры)
2. OCR штампа Timestamp Camera (нижние 28% фото — DD.MM.YYYY + HH:MM:SS)

Если ни EXIF, ни OCR не нашли дату+время — событие сохраняется с
`photo_timestamp=null`, `fraud_flags=['no_photo_time']`, `status='needs_review'`.
Бот немедленно отвечает работнику с просьбой переснять через Timestamp Camera.
`msg.date` (Telegram) не используется ни при каких условиях.

**Из самого фото (Face Recognition в ai-worker):**
- Лицо сотрудника → сравнивается с эталонным фото из базы
- Это главная **верификация подлинности**: на фото действительно тот,
  чьё имя указано в подписи?

### Поиск сотрудника с опечатками (fuzzy matching)

Рабочие часто пишут имя с ошибками. Система использует многошаговый поиск:

1. **Точное совпадение** (без учёта регистра): "иван петров" = "Иван Петров"
2. **Подстрока** (ilike): "иван" в "Иван Петров"
3. **Нечёткое совпадение** (порог 72% схожести): "Иван Пертов" → "Иван Петров"

Если совпадений нет → **сотрудник создаётся автоматически** (команда «Авто», ставка 0).
Руководитель вручную назначает ему роль, бригаду и ставку через страницу «Сотрудники».

### Защита от подделок

Система отслеживает потенциальные проблемы через **fraud_flags**. На дашборде они отображаются **русскими labels**:

| Флаг | Русское название | Причина | Действие |
|---|---|---|---|
| `face_mismatch` | Лицо не совпадает | На фото — другой человек, чем указано в подписи | needs_review → подтверждение руководителем |
| `no_face_detected` | Лицо не найдено | Face recognition не обнаружил лицо (лестница, предметы, фото предмета) | Событие отклоняется (status=duplicate) |
| `duplicate` | Дубль | Дублирующий departure без paired arrival в окне 18 часов | needs_review → проверка |
| `double_shift` | Двойная смена | Два полных цикла arrival→departure за один день | Все 4 события → needs_review → подтверждение или отклонение вторая пары |

Руководитель видит флаги:
- **В таблице Посещаемость:** иконка ⚠️ на ячейке дня
- **В карточках Проверка:** dropdown меню со всеми флагами события
- **На вкладке Табель:** в tooltip при наведении на день (если есть фото)

### Что НЕ используется

- **Проверка индекса объекта (postcode)** — проверка `wrong_location` отключена.
  Система работает с одним объектом, привязка через postcode штампа не нужна.
- **Штамп Timestamp Camera как обязательное требование** — штамп помогает
  извлечь время через OCR, но не обязателен. Система обработает фото без штампа,
  используя EXIF или время Telegram.

---

## Страницы Dashboard

Все страницы реализованы и доступны на `https://time-tracking-service-beta.vercel.app/`:

- **Обзор** — сводка за сегодня: кто на объекте, сколько needs_review, активность за день
  - **Интерактивные stat cards:** клик на карточку → навигация на соответствующую страницу (Посещаемость, Табель, Review)
- **Посещаемость** — таблица событий по сотрудникам за выбранный день с фильтрами
- **Проверка** — карточки needs_review для ручной корректировки
  - Подтверждение двойных смен (две пары arrival/departure за день)
  - Исправление часов с сохранением в Supabase
  - Кнопка "Проверить" на других вкладках → выделение сотрудника на этой вкладке
- **Табель** — помесячная/понедельная таблица по всем сотрудникам; экспорт Excel / PDF
  - **Мультифото tooltip при наведении на ячейку дня:**
    - **Нормальная смена:** 2 фото (приход + уход)
    - **Двойная смена:** 4 фото (2 прихода + 2 ухода)
    - Каждое фото подписано: "Приход HH:MM МСК" или "Уход HH:MM МСК"
    - Фото загружаются параллельно, кэшируются для быстрого отображения
- **Аналитика** — графики часов по дням, ФОТ по бригадам, таблица аномалий
- **Логи** — системный журнал с фильтрацией по источнику и уровню
- **Сотрудники** — управление базой: добавление, редактирование, эталонные фото, массовый импорт CSV

---

## Главный инструмент — страница «Табель»

### Роль
Основное рабочее место руководителя. Здесь формируется итоговый табель за период, отсюда данные уходят в бухгалтерию.

### Что видит руководитель на экране
Структура таблицы идентична привычному Excel:
- Слева — список всех сотрудников (ФИО, роль, ставка, ст-ть часа, переход часов)
- Сверху — дни периода
- В ячейках — отработанные часы или отметки «был / не был»
- Группировка по бригадам с зелёными подитогами
- Выходные подсвечены оранжевым
- Итоговые столбцы — часы и сумма

### Живая таблица (в отличие от Excel)
- Навёл мышку на день → всплыло фото сотрудника из Telegram
- Нераспознанные отметки подсвечены красным — сразу видно, где нужна проверка
- Поиск по ФИО прямо в таблице
- Бригады можно свернуть
- Выбор периода: день / неделя / месяц / произвольные даты

### Экспорт
Две кнопки:
- **Скачать Excel** — точная копия текущего файла для бухгалтерии (структура, листы, форматирование)
- **Скачать PDF** — для печати и подписи

Файл формируется прямо в браузере и скачивается.

### Как работает процесс
1. Руководитель открывает страницу «Табель», выбирает период
2. Dashboard подтягивает данные из Supabase
3. Формируется готовая таблица на экране (часы уже заполнены из распознавания фото)
4. Руководитель проверяет, при необходимости правит
5. Жмёт «Скачать Excel» → отправляет файл бухгалтеру по почте или мессенджеру

### Ключевое отличие от сегодняшнего дня
Часы в ячейках **заполняются автоматически** из фото через распознавание лиц. Руководитель не печатает — он **проверяет и подтверждает**.

### Принцип «одна роль»
Бухгалтерия **не заходит в систему**. Руководитель сам скачивает файл и передаёт — минимум сложности, никакого разграничения прав.

---

## Хранение данных и лимиты

### Где что хранится
- **`employees`** — сотрудники (имя, роль, бригада, ставка, эталонное фото, face embedding)
- **`events`** — отметки времени (кто, когда, приход/уход, часы)
- **`logs`** — журнал системных событий
- **Supabase Storage** — сами файлы фото из Telegram

### Как собирается таблица Табель
Таблица не хранится готовой. Каждый раз, когда руководитель открывает страницу «Табель», Dashboard запрашивает данные из Supabase (employees + events за период) и собирает таблицу в браузере на лету. Excel-файл формируется также в браузере при клике «Скачать».

### Лимиты Supabase Free Tier
- База данных: **500 МБ**
- Storage (фото): **1 ГБ**
- Трафик: **5 ГБ/мес**

### Ёмкость проекта (120 сотрудников)
- БД (тексты, события, логи): ~20 МБ/год → **хватит на 20+ лет**
- Storage без оптимизации: ~2,6 ГБ/мес → **не влезает в Free**

### Оптимизация Storage
- **Сжатие фото** при загрузке до ~150 КБ (качества достаточно для распознавания лиц)
- **Автоудаление** фото старше 30 дней
- Записи в БД (часы, даты, события) остаются навсегда — удаляются только файлы фото
- Активный объём: ~400–500 МБ → **вписывается в Free Tier**

### Когда может понадобиться Supabase Pro ($25/мес)
- Если клиент попросит хранить фото дольше 30 дней
- Pro даёт 100 ГБ Storage — хватит на 3+ года без сжатия

---

## Правила расчёта часов

Извлечены из Excel-файла заказчика (`Табель 23.03-08.04.xlsx`) через формулы и структуру ячеек.

### Базовые формулы

| Показатель | Формула | Пример |
|---|---|---|
| Норма рабочего дня | 8 часов | — |
| Стоимость часа | Ставка ÷ 8 | 5500 ÷ 8 = 687,5 |
| Часы за день | Время ухода − время прихода | 20:00 − 08:00 = 12 ч |
| Всего часов | Сумма часов за все дни периода | — |
| Сумма к выплате | Всего часов × Стоимость часа | 95 × 687,5 = 65 312,50 |

### Ключевые правила

- **Ставка = дневная** (не месячная). Оплата за полный 8-часовой день.
- **Обед не вычитается** — в ячейке полное время между первым «начало смены» и последним «конец смены».
- **«н»** в ячейке = не был на объекте, при суммировании считается как 0.
- **Выходные (сб, вс)** — рабочие дни на объекте, оплачиваются по той же ставке. Оранжевая подсветка — визуальная метка, на расчёт не влияет.
- **Дробные часы** допустимы (в файле встречается 12,5 часов).

### Переход часов

Между периодами сохраняется «переход часов» — сумма отработанных часов из предыдущего периода, копируется в первую колонку следующего:
- Первый период месяца (например, `23.03-31.03`) → перехода нет
- Следующий период (`01.04-08.04`) → в колонке «Переход часов» стоят итоги предыдущей недели

### Периоды и листы Excel

Экспорт формирует **отдельный лист Excel на каждый период**, обычно недельный:
- `23.03-31.03`
- `01.04-08.04`
- и т.д.

Это обязательно для совместимости с текущим процессом бухгалтерии.

### Бригады

Сотрудники группируются по бригадам. Между бригадами — **пустая строка-разделитель**, под каждой бригадой — **подитог суммой по бригаде** (зелёная подсветка). Имена бригад в файле не указаны, только структура.

---

## Чего не будет

- **Системы оплаты / расчёт зарплаты** — система фиксирует факт присутствия, но не считает деньги. Расчёт ФОТ остаётся в бухгалтерии / 1С.
- **Интеграция с 1С / ERP** — экспорт табеля в Excel/PDF есть, но прямой API-интеграции с учётными системами не будет.
- **HR-функции** — найм, увольнение, отпуска, больничные, графики смен вне скоупа.
- **GPS-трекинг** — система работает через фото в Telegram, не через геолокацию.
- **Видеонаблюдение** — только фото, не видеопоток и не камеры на объекте.
- **Управление задачами / нарядами** — это не таск-трекер и не система заданий.
- **Биометрические терминалы / железо** — никакого оборудования, только телефон и Telegram.
- **Мобильное приложение** — на старте интерфейс только через Telegram + веб-дашборд. Отдельное мобильное приложение возможно в будущем по запросу заказчика.
- **Множественные объекты** — на старте один объект. Поддержка нескольких объектов и привязка фото к объекту — в будущих итерациях.
- **Временные зоны** — один объект, один часовой пояс. Поддержка мультизонности не нужна.
- **Роли пользователей** — только руководитель. Собственник и бухгалтерия как отдельные роли с разграничением прав — не в скоупе.
- **Согласия ФЗ-152** — сотрудники уже отправляют фото в Telegram-группу добровольно. Юридическое оформление согласий на биометрию — пока не требуется.
- **Миграции БД / версионирование схемы** — Supabase CLI и формализованные миграции не используются. Схема правится напрямую.

---

## Стиль дизайна

- Минималистичный, деловой — без лишних украшений
- Акцент на читаемость таблиц и данных
- Цветовая схема: нейтральная (серый/белый) + один акцентный цвет
- Mobile-friendly (руководители работают с телефона)
- Компонентная библиотека: нет (чистый CSS + inline SVG, без внешних зависимостей)
